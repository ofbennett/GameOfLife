A C++ implementation of Conway's Game of Life. Different git branches in the repository parallelize the code in different ways (OpenMP, MPI, CUDA).

Using the make_video.py script to generate a video file for visualization requires an installation of ffmpeg on your system. 
